// write a function called findFirstLarger(arr, threshold) that returns 
// the first number in the array that is greater
// than the given threshold

const findFirstLarger = (arr, threshold) => arr.find(num => num > threshold);