// allows successive method calls on the same object or value
// especially on OO libraries and fluent APIs

// its often used w/ arrays, strings or custom APIs

[1, 2, 3]
    .map(x => x * 2)
    .filter(x => x > 2);