// functions as data structures

// example
oldList = [1, 2, 3];
const newList = oldList.append(4);

// once created, cannot be changed
// old version is still accessible
// new versions reuse parts of old ones