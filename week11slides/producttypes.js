// holds multiple values at the same time
// think of it as AND 

// getWeekAndDay :: String -> (Number x String)

// so the function above takes a string
// but it returns BOTH the number and the string

return [3, "Wednesday"];

// or

const point2D = {x: 5, y: 10}; // (Number x Number)